import { {{ pascalCase name }}Storyblok } from "@/lib/storyblok/component-types-sb";
import { SectionContainer } from "@/common/components/SectionContainer";
import { BlokWrapper } from "@/common/components/BlokWrapper";

export const {{ pascalCase name }} = ({ blok }: { blok: {{ pascalCase name }}Storyblok }) => {
  return (
    <BlokWrapper blok={blok}>
      <SectionContainer>
        Lorem ipsum
      </SectionContainer>
    </BlokWrapper>
  );
};
